#pragma once

#include "structs/execution_queue.h"

//  for  EXECUTION_QUEUE_RESPONSE* (*execute_instruction) (INSTRUCTION instruction);
EXECUTION_QUEUE_RESPONSE* execute_instruction(INSTRUCTION instruction) {
    
}